import { ArtistBuilder } from '$lib/metadata/artist';
import { LinkBuilder, Platform } from '$lib/metadata/link';

// TODO: Add links
const LINSANG = new ArtistBuilder().setName('LINRG').create();

export default LINSANG;
