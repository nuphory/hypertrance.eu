import { ArtistBuilder } from '$lib/metadata/artist';
import { LinkBuilder, Platform } from '$lib/metadata/link';

// TODO: Add links
const JEXXINIUM = new ArtistBuilder().setName('jexxinium').create();

export default JEXXINIUM;
